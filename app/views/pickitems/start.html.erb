<% session[:pickgroup] = nil %>
<% session[:shipper] = nil %>

<div class="start-main">
  <div class="start">
    <div class="start-image"><%= image_tag("pickapp.jpg", class:"pickapp-icon") %></div>
    <div class="start-items">
      <div class="start-text">
        <div class="start-text__main">しゅうひん君</div>       
        <%= form_with url:pickitems_path, method: :post, class:"start-text__sub", local: true do |form| %>
        <div class="start-text__">
          <%= form.select :created_at,options_for_select(Pickitem.all.group_by{ |item| item.created_at.to_date }.sort_by{ |to_date, _| to_date }.reverse),
            class:"start-input", prompt: "今日の日付を選択してください", required: true %>
        </div>
        <div class="start-text__">
          <%= form.label :shipper, "担当者コード入力" %>
          <%= form.text_field :shipper, type: "number", class:"start-input", type: "number", required: true %>
        </div>
        <div class="start-text__">
          <%= form.label :pickgroup, "ピッキングNo選択" %>
          <%= form.select :pickgroup, options_for_select(Pickitem.all.group_by(&:pickgroup).sort_by{ |group, items| group}.map { |group, items| ["#{group} - (#{items.count}行)", group] }), prompt: "選択してください", class:"start-input" %>
        </div>
          <%= form.submit "Start", class:"service-btn" %>
        <% end %> 
      </div>
    </div>
  </div>
</div>